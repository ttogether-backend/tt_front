import Page from 'src/pages/layout';

const LoginForm = () => {
	return (
		<Page>
			<div
				style={{
					display: 'flex',
					justifyContent: 'center',
				}}>
			</div>
		</Page>
	);
};

export default LoginForm;