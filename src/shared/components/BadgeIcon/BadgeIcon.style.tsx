import { styleType } from "./BadgeIcon.types";
import styled from '@emotion/styled';

//subType 종류 : auth1(1차인증완료), auth2(2차인증완료), zero / zeroSmall, 
export const BadgeIcon = styled.div<styleType>`

`;